@model com.web.ViewModels.NewProductViewModels
@{ViewBag.Title = "Create";}

<div class="modal-body">
    <div id="cProduct" class="title-section">
        <h2 class="title">
            Create Product
        </h2>
    </div>

    <div class="row">
        <div class="col-md-6">

            <form id="createProductForm">

                <div class="form-group">
                    <label for="Name" class="control-label">Name</label>
                    <input type="text" name="Name" placeholder="Product Name" class="form-control" />
                </div>

                <div class="form-group">
                    <label for="Price" class="control-label">Price</label>
                    <input type="number" name="Price" placeholder="Product Price" class="form-control" />
                </div>

                <div class="form-group">
                    <label for="Description" class="control-label">Description</label>
                    <textarea rows="3" class="form-control" placeholder="Product Description" name="Description"></textarea>
                </div>

                <select class="form-control" name="CategoryID">
                    <option value="SelectCategory">SelectCategory</option>
                    @foreach (var item in Model.CategoryList)
                     {
                        <option value="@item.ID">@item.Name</option>
                     }
                </select>
                
                <div class="form-group">
                    <input type="file" id="imageUpload" class="form-control" name="Image" accept=".png, .jpg" />
                    <input name="imageUrl" id="imageUrl" type="hidden" />
                </div>

                <div class="form-group">
                    <label>Is Featured?</label>
                    <div class="input-group">
                        <div class="radio form-control">
                            <label><input name="isFeatured" type="radio" value="True" /> Yes</label>
                        </div>
                        <div class="radio form-control">
                            <label><input name="isFeatured" type="radio" value="False" /> No</label>
                        </div>
                    </div>
                </div>

                <div class="form-group">
                    <button type="button" id="saveproduct" class="btn btn-primary">Submit</button>
                </div>
            </form>

        </div>

        <div class="col-md-6">
                <div class="card">
                    <div class="col-md-8 text-center">
                        <img id="categoryImage" src="~/Content/images/noOne.png" class="img-thumbnail img-fluid " />
                    </div>
                </div>
        </div>

    </div>
</div>
<script>
    $('#createProductForm').validate({
        rules:
        {
            Name: {
                required: true,
                minlength: 3,
                maxlength: 50,
            },
            Price: {
                required: true,
                //range: [1 - 10000]
            },
            CategoryID: {
                required: true,
            },
            Description: {
                maxlength: 500
            }
        },
        messages:
        {
            Name: {
                required: "<span class='text-danger'>Name is required</span>",
                minlength: "<span class='text-danger'>Minimum Length is 3 </span>",
                maxlength: "<span class='text-danger'>Maximum Length is 50</span>"
            },
            Price:
            {
                required: "<span class= 'text-danger' > Price is required</span>"
            },
            CategoryID:
            {
                required: "<span class= 'text-danger' > Please Select Category</span>"
            },
            Description: {
                maxlength: "<span class='text-danger'>Maximum Length is 500</span>"
            }
        }
    });
</script>